# SMATournament v0.1（legacy）

localStorage ベースの試作版です。**Firebase 版（リポジトリルート）からは読み込まれません。**

参照・比較用に `_legacy/` へ退避しています。

## 起動

```powershell
cd C:\Users\user2\Projects\SMATournament\_legacy
npx --yes serve -p 8766
```

http://localhost:8766/index.html
